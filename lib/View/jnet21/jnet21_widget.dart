import 'package:flutter/material.dart';

class Jnet21Widget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('JNET21のページです。', style: TextStyle(fontSize: 20)),
      ),
    );
  }
}
